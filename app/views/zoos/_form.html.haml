= form_for @zoo, html: { role: "form" } do |f|
  - if @zoo.errors.any?
    #error_explanation
      %h2
        = pluralize(@zoo.errors.count, "error")
        prohibited this zoo from being saved:

      %ul
      - @zoo.errors.full_messages.each do |msg|
        %li
          = msg

  .row
    .col-lg-3
      .form-group
        = f.label :name
        %br
        = f.text_field :name, class: "form-control"

  .actions
    = f.submit class: "btn btn-default"

