%h1 Zoo List

- if @zoos.size > 0
  %table.table-hover.table-striped.table-bordered
    %thead
      %tr
        %th Name
        %th Unique Species
        %th{colspan:4}

    %tbody
      - @zoos.each do |zoo|
        %tr
          %td #{zoo}
          %td #{zoo.unique_species_count}
          %td
            = link_to "Show", zoo
          %td
            = link_to "Edit", edit_zoo_path(zoo)
          %td
            = link_to "Manage Animals", zoo_animals_path(zoo)
          %td
            = link_to "Delete", zoo, method: :delete, data: { confirm: "Are you sure you want to remove this zoo and all its animals?" }
- else
  %p There are currently no zoos

%br

= link_to "Add a New Zoo", new_zoo_path
